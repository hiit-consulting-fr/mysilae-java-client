<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>mysilae-java-client</artifactId>
    <groupId>fr.hiitconsulting</groupId>
    <version>v2-SNAPSHOT</version>

    <name>${project.artifactId}</name>

    <properties>
        <java.version>25</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dependency versions -->
        <ms-parent.version>1.30.0</ms-parent.version>
        <jackson-databind-nullable.version>0.2.6</jackson-databind-nullable.version>

        <!-- Plugin versions -->
        <maven-clean-plugin.version>3.5.0</maven-clean-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <build-helper-maven-plugin.version>3.6.1</build-helper-maven-plugin.version>
        <openapi-generator-maven-plugin.version>7.18.0</openapi-generator-maven-plugin.version>
    </properties>

    <scm>
        <url>https://github.com/hiit-consulting-fr/mysilae-java-client</url>
    </scm>

    <issueManagement>
        <url>https://github.com/hiit-consulting-fr/mysilae-java-client</url>
    </issueManagement>

    <ciManagement>
        <url>https://github.com/hiit-consulting-fr/mysilae-java-client/actions</url>
    </ciManagement>

    <organization>
        <name>Hi!t Consulting</name>
        <url>https://www.hiit-consulting.fr</url>
    </organization>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>fr.hiitconsulting</groupId>
                <artifactId>ms-parent</artifactId>
                <version>${ms-parent.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!-- Spring -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Others -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${maven-source-plugin.version}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>${maven-clean-plugin.version}</version>
                <configuration>
                    <filesets>
                        <fileset>
                            <directory>gen</directory>
                            <excludes>
                                <exclude>**/.openapi-generator-ignore</exclude>
                                <exclude>**/maven.config</exclude>
                            </excludes>
                        </fileset>
                    </filesets>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <version>${openapi-generator-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>generate.mysilae.api.client</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>swagger-api.yml</inputSpec>
                            <output>${project.basedir}/gen</output>
                            <generatorName>java</generatorName>
                            <apiPackage>${project.groupId}.mysilae.api.client</apiPackage>
                            <modelPackage>${project.groupId}.mysilae.api.client.model</modelPackage>
                            <invokerPackage>${project.groupId}.mysilae.invoker</invokerPackage>
                            <groupId>${project.groupId}</groupId>
                            <artifactId>mysilae-java-client</artifactId>
                            <artifactVersion>${project.version}</artifactVersion>
                            <skipValidateSpec>true</skipValidateSpec>
                            <configOptions>
                                <dateLibrary>java8</dateLibrary>
                                <library>resttemplate</library>
                                <useJakartaEe>true</useJakartaEe>
                                <openApiNullable>false</openApiNullable>
                            </configOptions>
                        </configuration>
                    </execution>
                    <execution>
                        <id>generate.mysilae.auth.api.client</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>swagger-auth.yml</inputSpec>
                            <output>${project.basedir}/gen</output>
                            <generatorName>java</generatorName>
                            <apiPackage>${project.groupId}.mysilae.auth.client</apiPackage>
                            <modelPackage>${project.groupId}.mysilae.auth.client.model</modelPackage>
                            <invokerPackage>${project.groupId}.mysilae.invoker</invokerPackage>
                            <groupId>${project.groupId}</groupId>
                            <artifactId>mysilae-java-client</artifactId>
                            <artifactVersion>${project.version}</artifactVersion>
                            <skipValidateSpec>true</skipValidateSpec>
                            <configOptions>
                                <dateLibrary>java8</dateLibrary>
                                <library>resttemplate</library>
                                <useJakartaEe>true</useJakartaEe>
                                <openApiNullable>false</openApiNullable>
                            </configOptions>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>${build-helper-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>add-generated-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.basedir}/gen/src/main/java</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Hi!t Consulting Apache Maven Packages</name>
            <url>https://maven.pkg.github.com/hiit-consulting-fr/mysilae-java-client</url>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2</url>
        </repository>
        <repository>
            <id>github</id>
            <url>https://maven.pkg.github.com/hiit-consulting-fr/ms-parent</url>
        </repository>
    </repositories>
</project>